
describe('subjectsController', () => {

    beforeAll(async () => {
    });

    beforeEach(() => {
    });

    test('should return all subjects with status 200', async () => {
        //given 
        //when
        //then
    });

    test('should call next with error if getAll fails', async () => {
        //given 
        //when
        //then
    });
});